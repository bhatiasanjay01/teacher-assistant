// The resource name is used in `in-memory-database` and `service`
export enum ResourceName {
  test = 'test',
  userAuth0 = 'user-auth0',
  file = 'file',
  openai = 'openai',
  email = 'email',
  websocket = 'websocket',
  langchain = 'langchain',
}
